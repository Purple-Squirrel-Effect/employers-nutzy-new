---
import { getCollection } from "astro:content";
import ContactSection from "../../components/ContactSection.astro";
import EventCalendar from "../../components/EventCalendar.astro";
import App from "../../layouts/App.astro";

export async function getStaticPaths({ paginate }) {
  // Get all blog posts from the content collection
  const allPosts = await getCollection("blog");
  
  // Sort posts by date (newest first)
  const sortedPosts = allPosts
    .sort((a, b) => new Date(b.data.posted).getTime() - new Date(a.data.posted).getTime());

  // Return paginated paths
  return paginate(sortedPosts, { pageSize: 6 });
}

// The page prop contains all the paginated data
const { page } = Astro.props;

// Format date for display
function formatDate(date: Date): string {
  return date.toLocaleDateString("nl-NL", {
    year: "numeric",
    month: "long",
    day: "numeric",
  });
}

// Calculate reading time (simple word count estimation)
function calculateReadingTime(content: string): number {
  const words = content.replace(/<[^>]*>/g, "").split(/\s+/).length;
  return Math.ceil(words / 200); // 200 words per minute
}

// Get excerpt from content - prioritize description field, fallback to content
function getExcerpt(post: any, maxLength: number = 150): string {
  // First try to use the description field if it exists
  if (post.data.description && post.data.description.trim()) {
    return post.data.description.trim();
  }

  // Fallback to content excerpt
  const cleanContent = post.data.content.replace(/<[^>]*>/g, "").trim();
  return cleanContent.length > maxLength
    ? cleanContent.substring(0, maxLength) + "..."
    : cleanContent;
}
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>
      Nutzy - Gen-Z Recruitment Platform | Visuele Vacatures & Creator Network
    </title>
    <meta
      name="description"
      content="Bereik Gen-Z talent met Nutzy's visuele recruitment platform. Van visuele vacatures tot creator partnerships - transformeer jouw recruitment aanpak."
    />
  </head>
  <body>
    <App>
      <section
        class="min-h-screen bg-primary-500 gap-8 px-8 md:px-16 lg:px-32 items-center justify-center grid grid-cols-1 lg:grid-cols-2"
      >
        <img
          class="absolute top-0 opacity-5020 z-0 h-[200svh] w-[200svh]"
          src="/svg/particle/share.svg"
        />

        <div class="order-2 lg:order-1 relative z-10 rounded-full">
          <h1
            class="text-5xl text-shadow shadow-black text-8xl font-bold text-white relative z-50"
          >
            Kennis delen
          </h1>
          <p class="text-xl text-white mt-6 max-w-2xl">
            Bereik de juiste doelgroep met onze strategische campagnes speciaal
            ontworpen voor het werven van Gen-Z talent. Van concept tot
            uitvoering - wij zorgen voor resultaat.
          </p>
          <div class="mt-8 flex flex-wrap gap-4">
            <a
              href="#strategy"
              class="px-8 py-4 border-2 border-current font-bold rounded-lg hover:bg-secondary-400 transition-all duration-300"
            >
              Ontdek onze aanpak
            </a>
            <a
              href="#contact"
              class="px-8 py-4 bg-tertiary-500 text-black font-bold rounded-lg hover:brightness-105 transition-all duration-300"
            >
              Neem contact op
            </a>
          </div>
        </div>
        <div class="order-2">
          <!-- <img class="h-screen w-screen relative z-20" src="/svg/a-group-of-people-sharing-ideas.svg" /> -->
        </div>
      </section>
      <section id="blog-content" class="relative px-8 md:px-16 lg:px-32 mx-auto my-16" style="scroll-margin-top: 2rem;">
        <div class="mx-auto">
          <!-- Section Header -->
          <div class="text-center mb-12">
            <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
              Blog Posts {page.currentPage > 1 && `- Pagina ${page.currentPage}`}
            </h2>
            <p class="text-lg text-gray-600 max-w-2xl mx-auto">
              Ontdek de nieuwste inzichten, trends en strategieën voor moderne recruitment
            </p>
          </div>

          <!-- Blog Posts Grid -->
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {page.data.map((post) => (
              <a
                href={`/blog/${post.id}`}
                class="block group focus:outline-none focus:ring-4 focus:ring-primary-200 focus:ring-offset-2 rounded-2xl transition-all duration-300"
              >
                <article class="bg-white border border-gray-100 rounded-2xl shadow-lg hover:shadow-2xl hover:border-primary-200 transition-all duration-300 hover:scale-[1.02] overflow-hidden h-full flex flex-col">
                  <!-- Post Header -->
                  <div class="p-6 pb-4 flex-1">
                    <!-- Category Badge -->
                    <div class="mb-4">
                      <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-primary-100 text-primary-800 border border-primary-200">
                        {post.data.category}
                      </span>
                    </div>

                    <!-- Title -->
                    <h3 class="text-xl font-bold text-gray-900 mb-3 line-clamp-2 group-hover:text-primary-700 transition-colors leading-tight">
                      {post.data.title}
                    </h3>

                    <!-- Excerpt -->
                    <div class="text-gray-600 mb-4 line-clamp-3 leading-relaxed">
                      {post.data.description ? (
                        <div set:html={post.data.description} />
                      ) : (
                        <p>{getExcerpt(post)}</p>
                      )}
                    </div>
                  </div>

                  <!-- Post Footer -->
                  <div class="px-6 pb-6 mt-auto">
                    <div class="flex items-center justify-between text-sm text-gray-500">
                      <!-- Author -->
                      <div class="flex items-center gap-3">
                        <div class="h-10 w-10 bg-gradient-to-br from-primary-500 to-primary-600 rounded-full flex items-center justify-center shadow-md">
                          <span class="text-white font-bold text-sm">
                            {post.data.author.split(' ').map(name => name[0]).join('')}
                          </span>
                        </div>
                        <div class="flex flex-col">
                          <span class="font-medium text-gray-700">{post.data.author}</span>
                          <span class="text-xs text-gray-500">{formatDate(post.data.posted)}</span>
                        </div>
                      </div>

                      <!-- Reading Time -->
                      <div class="flex items-center gap-2 text-xs text-gray-500 bg-gray-50 px-3 py-1 rounded-full">
                        <svg class="h-3 w-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                        </svg>
                        <span>
                          {calculateReadingTime(post.data.content)} min
                        </span>
                      </div>
                    </div>
                  </div>
                </article>
              </a>
            ))}
          </div>

          <!-- Pagination Controls -->
          <div class="flex justify-center items-center gap-2 mt-12">
            <!-- Previous Page Button -->
            {page.url.prev && (
              <a
                href={`${page.url.prev}#blog-content`}
                class="inline-flex items-center gap-2 px-4 py-2 bg-gray-100 text-gray-700 font-medium rounded-lg hover:bg-gray-200 transition-all duration-300"
              >
                <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                </svg>
                Vorige
              </a>
            )}

            <!-- Page Numbers -->
            <div class="flex gap-1">
              {Array.from({ length: page.lastPage }, (_, i) => i + 1).map((pageNum) => (
                <a
                  href={pageNum === 1 ? "/blog#blog-content" : `/blog/${pageNum}#blog-content`}
                  class={`px-4 py-2 rounded-lg font-medium transition-all duration-300 ${
                    pageNum === page.currentPage
                      ? "bg-primary-500 text-white"
                      : "bg-gray-100 text-gray-700 hover:bg-gray-200"
                  }`}
                >
                  {pageNum}
                </a>
              ))}
            </div>

            <!-- Next Page Button -->
            {page.url.next && (
              <a
                href={`${page.url.next}#blog-content`}
                class="inline-flex items-center gap-2 px-4 py-2 bg-gray-100 text-gray-700 font-medium rounded-lg hover:bg-gray-200 transition-all duration-300"
              >
                Volgende
                <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </a>
            )}
          </div>
        </div>
      </section>

      <section>
        <div class="w-full z-20 relative my-16">
          <EventCalendar />
        </div>
      </section>
      <ContactSection />
    </App>
  </body>
</html>
